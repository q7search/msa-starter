spring:
  cloud:
    config:
      uri: http://127.0.0.1:8888 #config server
      name: config-service
  profiles:
    active: dev

management:
  endpoints:
    web:
      exposure:
        include: refresh, health, beans, httptrace
